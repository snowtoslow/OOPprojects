package com.snowtoslow.filereader;

import java.io.IOException;

public interface FileReader {

    //String readFileAsString(String path) throws Exception;
    char addToNumarator = '(';
    char substractFromNumarator = ')';
    String positiveResult = " is mathematically  correct, because NUMARATOR == ";
    String negativeResult = " is mathematically not correct, because NUMARATOR == ";


}
